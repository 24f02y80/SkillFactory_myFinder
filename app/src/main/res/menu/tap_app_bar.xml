<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/fav"
        android:icon="@drawable/ic_round_favorite"
        android:title="@string/btn_fav"
        app:showAsAction="withText"/>
    <item
        android:id="@+id/watch_later"
        android:icon="@drawable/ic_round_watch_later"
        android:title="@string/btn_watch_later"
        app:showAsAction="withText"/>
    <item
        android:id="@+id/selection"
        android:icon="@drawable/ic_round_collections"
        android:title="@string/btn_selections"
        app:showAsAction="withText"/>
</menu>